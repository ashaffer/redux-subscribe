{
  "name": "@ashaffer/redux-subscribe",
  "description": "Subscribe to path in your redux state atom",
  "repository": "git://github.com/ashaffer/redux-subscribe.git",
  "version": "0.1.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "prepublish": "make build",
    "postpublish": "make clean",
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@f/foreach": "^1.2.2",
    "@f/get-prop": "^1.1.1"
  },
  "devDependencies": {
    "@f/set-prop": "^1.0.0",
    "babel-preset-es2015": "^6.1.2",
    "babel-tape-runner": "^2.0.1",
    "redux": "^3.3.1",
    "tape": "^4.2.0"
  }
}
